# M3.5: Connectivity Hardening Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Six surgical fixes to the hardware-validated connectivity stack: cold-start STA race (#47), post-crash AP-restore budget (#48), DHCP lease renewal (#46), open setup AP (#44), Europe/London time (#45), NTP daemon mode (#50).

**Architecture:** No structural rework — each fix lands at its site with the manager's existing idioms (injected sleeper/clock seams, FakeRunner tests, verified-AP-restore discipline). One new micro-package `internal/tz`. Spec: `docs/superpowers/specs/2026-07-08-m3.5-connectivity-hardening-design.md`.

**Tech Stack:** Go stdlib (+`time/tzdata` embed). Bash for flash-sd.

## Global Constraints

- Branch `feat/m3.5-hardening`. TDD red→green per task; `make check` exit 0 before any push.
- `internal/web` ↔ `internal/net` import ban unchanged; no `os/exec` outside `internal/net/runner.go`; no raw `time.Sleep`/`time.Now` in `internal/net` logic (injected seams only).
- #47 fast retry: ONCE per process lifetime, only when the FIRST attempt fails within 2s (injected clock). #48 AP-restore retry: exactly ONE full toAP retry before the existing fatal path; "verified AP restore" invariant holds (verified on the retry).
- #46: dhclient argv `dhclient -v -pf /run/trainboard-dhclient.pid wlan0` (no `-1`); kill-before-start every attempt; kill on teardown/AP transition/shutdown.
- #44: AP block `key_mgmt=NONE`, no `psk`; `board.Hotspot` loses `Password`; `eval-mode2.sh --ap-pass` UNTOUCHED. Only hotspot-scene goldens may change.
- #45: all display/comparison time in `Europe/London` via `internal/tz.Location()`; `import _ "time/tzdata"` lives ONLY in internal/tz; never panic (UTC fallback + logged error).
- The d23a7d7/ac67591 watchdog regression tests must stay green through every task.

---

### Task 1: #47 — ensureDaemon socket wait + manager fast-retry-once

**Files:**
- Modify: `internal/net/driver_mode2.go:99-115` (ensureDaemon), `internal/net/manager.go` (run loop first-attempt handling, ~line 248-278)
- Test: `internal/net/driver_mode2_test.go`, `internal/net/manager_test.go` (append)

**Interfaces:**
- Produces: `ensureDaemon` returns `(started bool, err error)` — `started=true` iff it spawned `wpa_supplicant -B` this call (Task 2 consumes `started` for the StartAP budget). Existing callers updated (`StartAP`, `AttemptSTA` — grep `ensureDaemon(` in driver_mode2.go).
- Manager gains unexported field `fastRetried bool` guarded by the run-loop's single-goroutine ownership (no mutex — run loop only).

Behavior:
- ensureDaemon, after a spawn: poll `wpa_cli -i <iface> status` up to 10×500ms via the driver's injected sleeper (grep how pollStatus sleeps — same seam) until it exits 0; timeout → `(true, error("daemon ctrl socket not ready after 10 polls"))`.
- Manager run loop (the `toSTA` call site at manager.go:268): record attempt start from the injected clock; on error, if `!m.fastRetried && attemptDuration < 2*time.Second && this is the first STA attempt of the process` (a `firstAttempt bool` initialized true, cleared after any attempt): set `fastRetried=true`, log `"first STA attempt failed instantly; fast retry"`, immediately re-run `toSTA` once; only then fall to AP fallback. The 2s threshold is a package const `fastRetryThreshold = 2 * time.Second`.

- [ ] **Step 1: Failing tests.** driver: sequence-aware runner (in-file stub precedent: `sequencedStatusRunner` in driver_mode2_test.go from M3b Task 7) scripted so post-spawn `wpa_cli status` fails twice then succeeds → ensureDaemon returns `(true, nil)` and issued exactly one `wpa_supplicant -B`; scripted all-fail → `(true, err)` after exactly 10 status polls. manager: fake driver whose first AttemptSTA errors instantly (fake clock advances 0s) → assert exactly TWO AttemptSTA calls before hotspot publishes (fast retry) and log line present; second scenario: first attempt errors after fake-clock +3s → exactly ONE call then AP fallback (no fast retry); third: fast retry itself fails → AP fallback, and a LATER cycle's instant failure gets NO fast retry (once per lifetime pinned).
- [ ] **Step 2: Red** — `go test ./internal/net/ -run 'EnsureDaemon|FastRetry'` FAIL.
- [ ] **Step 3: Implement.** **Step 4: Green** — `go test -race ./internal/net/` (d23a7d7/ac67591 tests must pass untouched).
- [ ] **Step 5: Commit** — `fix(net): ensureDaemon waits for ctrl socket; manager fast-retries an instant first STA failure once (#47)`

---

### Task 2: #48 — post-restart StartAP budget + one AP-restore retry

**Files:**
- Modify: `internal/net/driver_mode2.go:117-140` (StartAP), `internal/net/manager.go` (AP-fallback fatal path — grep `AP restore failed`)
- Test: `internal/net/driver_mode2_test.go`, `internal/net/manager_test.go` (append)

**Interfaces:**
- Consumes Task 1's `ensureDaemon() (started bool, err error)`.
- StartAP: when `started` is true, AP-active poll budget becomes 20×500ms (const `apPollsAfterDaemonStart = 20`; existing budget stays as its current const — read StartAP for the exact name).
- Manager: the code path that currently returns the fatal `"AP fallback retry: AP restore failed"` error instead first logs `"AP restore failed; one in-process retry"` and re-runs the full `toAP` transition ONCE (fresh teardown first — reuse the existing transition entry point, do not inline a partial copy); second failure → existing fatal return verbatim (watchdog backstop unchanged).

- [ ] **Step 1: Failing tests.** driver: scripted runner where ensureDaemon spawns (status fails then succeeds per Task 1) and AP-active polls succeed only on poll 15 → StartAP succeeds (20-budget); same but daemon already running → fails at 10 polls (old budget pinned). manager: fake driver failing toAP once then succeeding → manager publishes hotspot, no exit, retry log present; failing toAP twice → manager exits with the existing fatal error string (assert `errors.Is`/message match — read the current test for the fatal path and extend it).
- [ ] **Step 2: Red.** **Step 3: Implement.** **Step 4: Green** (`-race`, full package). **Step 5: Commit** — `fix(net): AP restore gets post-daemon-start poll budget + one in-process retry before fatal (#48)`

---

### Task 3: #46 — daemon-mode dhclient with owned lifecycle

**Files:**
- Modify: `internal/net/driver.go:83-115` (staAttempt), `internal/net/driver_mode2.go` + `internal/net/driver_hostapd.go` (teardown/AP-transition paths — grep existing `pkill`/teardown verbs to find where STA state is dismantled)
- Test: `internal/net/driver_test.go`, `internal/net/driver_mode2_test.go`

**Interfaces:**
- staAttempt's dhclient argv becomes exactly: `dhclient -v -pf /run/trainboard-dhclient.pid wlan0` (iface param as today; pidfile const `dhclientPidfile = "/run/trainboard-dhclient.pid"`).
- New unexported helper in driver.go: `killDHClient(ctx, r Runner)` — `pkill -F /run/trainboard-dhclient.pid` tolerating exit-1 (not running), removing nothing else. Called: (a) at the START of every staAttempt (kill-before-start), (b) in every driver path that leaves STA mode for AP (both drivers — find the select_network/hostapd-start transitions), (c) manager shutdown path if it doesn't already route through (b) (read manager.go's ctx-done teardown; extend the driver teardown it calls, not the manager).

- [ ] **Step 1: Failing tests.** Assert argv: no `-1`, pidfile flag present (extend the existing staAttempt happy-path test's argv assertions — grep `dhclient` in driver tests); kill-before-start: FakeRunner sees `pkill -F` BEFORE the conf write in a fresh attempt; AP transition: mode2 toAP sequence includes the pkill; pkill exit-1 tolerated (scripted failure → attempt proceeds).
- [ ] **Step 2: Red.** **Step 3: Implement.** **Step 4: Green.** **Step 5: Commit** — `fix(net): dhclient runs as renewing daemon with driver-owned pidfile lifecycle (#46)`

---

### Task 4: #44 — open setup AP, Password removed end-to-end

**Files:**
- Modify: `internal/net/driver_mode2.go` (renderConf AP block), `internal/net/driver_hostapd.go` (conf parity), `internal/board/snapshot.go:42` (Hotspot struct), `internal/board/scenes.go` (hotspot scene), `cmd/trainboard/connectivity.go` (resolveAPPassword deleted; manager construction), `cmd/trainboard/main.go` (E04 path parity), `internal/config/appassword.go` (DELETE), `internal/config/config.go` (ProvisioningConfig.APPassword field + persistence — grep `APPassword`), `internal/web` (RegenerateAPPassword action + template mentions + status view + setup copy — grep -ri appassword + "hotspot" in templates), `deploy/bench/destructive-matrix.md`, `docs/deploy.md` (AP-password mentions)
- Test: every touched package's tests; goldens: ONLY hotspot-scene files may change (regenerate via the goldens rule in the board tests — read the golden-update convention in internal/board first)

**Interfaces:**
- `board.Hotspot` becomes `{SSID, Addr string}` — compile ripples through web Sources/fakes and composite tests; fix mechanically.
- mode2 AP network block: `key_mgmt=NONE`, no `psk=`; hostapd conf drops the `wpa=`/`wpa_passphrase` lines.
- Web: `POST /actions/regen-ap-password` route + API mirror REMOVED (routes DELETED → e2e route-matrix rows deleted — the tripwire test must be updated to match, and the matrix count assertion with it).

- [ ] **Step 1: Failing tests first for the conf renderers** (AP block asserts `key_mgmt=NONE` present, `psk=` absent), **then mechanical compile-and-fix outward** (struct field removal is compiler-driven; run `go build ./...` and fix every site), **then the deletions** (appassword.go, regen route, matrix rows) with their tests. E2E security sweep: grep templates/responses for any residual "password" copy in hotspot/AP contexts.
- [ ] **Step 2-4: Red → implement → Green** across `./internal/... ./cmd/...`; goldens rule: hotspot-scene only, anything else = STOP and report.
- [ ] **Step 5: Commit** — `feat(net,board,web,config): open setup AP — WPA2 password removed end-to-end (#44)`

---

### Task 5: #45 — internal/tz + threading

**Files:**
- Create: `internal/tz/tz.go`, `internal/tz/tz_test.go`
- Modify: `internal/render/element_clock.go:22`, `internal/config/powersaving.go:15-16` (+ its callers passing time.Now — grep `Powersaving` consumers), `internal/data/timerecon.go:45`, `docs/deploy.md` (host-TZ-irrelevant note)
- Test: each package's tests

**Interfaces:**
```go
package tz // import _ "time/tzdata" lives here and ONLY here

// Location returns Europe/London, resolving via the embedded tzdata when
// the host zoneinfo is missing. Never panics: on any error it logs once
// (slog.Default) and returns time.UTC.
func Location() *time.Location  // sync.Once-cached
```
- element_clock: `now.In(tz.Location()).Format("15:04")`. powersaving: comparisons computed in `tz.Location()` (the window is UK wall-clock). timerecon: Darwin HH:MM parsed against a reference `now.In(tz.Location())` (read the function — it builds same-day timestamps; the reference day/DST must be London's).

- [ ] **Step 1: Failing tests.** tz: Location() returns a zone whose name is "Europe/London" (or UTC fallback path exercised via... the fallback is untestable without breaking the embed — assert the happy path + document); clock/powersaving/timerecon: table tests with fixed `time.Date(...)` instants either side of a BST boundary (2026-03-29 01:00 UTC) asserting London wall-clock behavior (e.g. clock renders 01:59 UTC as "02:59" pre-flip vs "01:59"... compute the correct expectations from the actual DST rule when writing).
- [ ] **Step 2: Red.** **Step 3: Implement.** **Step 4: Green** + `make check` (binary-size delta noted in the report, ~450KB accepted). **Step 5: Commit** — `feat(tz,render,config,data): Europe/London wall-clock everywhere times are shown or compared (#45)`

---

### Task 6: #50 — flash-sd NTP mode + deploy.md; branch docs sweep

**Files:**
- Modify: `deploy/flash-sd.sh` (dietpi.txt write — grep `CONFIG_` to find the block), `docs/deploy.md` §8 (migration checklist gains the NTP requirement)

- [ ] Steps: flash-sd writes `CONFIG_NTP_MODE=4` (with a comment: boot-only sync never completes under manager-owned wlan0; no RTC) → `bash -n` + shellcheck on flash-sd.sh → deploy.md §8 checklist item ("switch NTP to daemon mode BEFORE enabling --manage-network: `sed -i 's/CONFIG_NTP_MODE=./CONFIG_NTP_MODE=4/' /boot/dietpi.txt && systemctl enable --now systemd-timesyncd`") + note the bench Pi is already converted → `make check` → commit `fix(deploy): NTP daemon mode required under manager-owned wlan0 (#50)`

---

## Post-plan (not tasks)

- Final whole-branch review (opus) → PR → Jess merges.
- On-hardware verification over the usb0 lifeline (no dead-man): config-apply joins in seconds not minutes (#47); `pkill -9 wpa_supplicant` mid-AP recovers in-process ≤~30s (#48); lease renewal observed across an expiry (#46); open AP joins without password + glass shows no password line (#44); glass clock right (#45).
- Close #44-#48, #50 with evidence.

## Self-review notes

- Spec coverage: #47 ✅ T1; #48 ✅ T2; #46 ✅ T3; #44 ✅ T4; #45 ✅ T5; #50 ✅ T6; out-of-scope items in no task ✅.
- Type consistency: `ensureDaemon() (bool, error)` produced T1, consumed T2; `dhclientPidfile`/`killDHClient` self-contained in T3; `tz.Location()` produced/consumed T5.
- Placeholders: none. T4 is deliberately compiler-driven after the renderer TDD (field removal), with explicit grep verbs for the sweep; T5's DST expectations instruct computing from the real rule rather than guessing.
